{
    "title": "First Non-Repeating Character",
    "description": "Given a string x, find the first non-repeating character and return it as a string. If it doesn't exist, return an empty string.",
    "inputFormat": "A string x",
    "outputFormat": "A string of length 1",
    "constraints": "1 <= x.length <= 1000",
    "gameDifficulty": "EASY",
    "gameLanguage": "JAVA",
    "sampleSolution": "import java.util.Arrays;\nimport java.util.HashMap;\nimport java.util.HashSet;\n\nclass Solution {\n    public static String solve(String x) {\n        HashMap<Character, Integer> counts = new HashMap<>();\n        for (char c : x.toCharArray()) {\n            counts.put(c, counts.getOrDefault(c, 0) + 1);\n        }\n        for (char c : x.toCharArray()) {\n            if (counts.get(c) == 1) return String.valueOf(c);\n        }\n        return \"\";\n    }\n}",
    "hint": "You can use a hash map to count occurrences of characters",
    "testCases": [
        { "input": "swiss", "expectedOutput": "w" },
        { "input": "aabbccde", "expectedOutput": "d" },
        { "input": "programming", "expectedOutput": "p" },
        { "input": "hello", "expectedOutput": "h" },
        { "input": "a", "expectedOutput": "a" }
    ]
}